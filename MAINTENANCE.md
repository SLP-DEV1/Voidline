# Maintenance

Voidline is a binary-only release repository. Keep maintenance focused on release integrity, compatibility, documentation, issue quality, and packaging.

For new public versions, use a `v*` Git tag so the release workflow can package the tracked runtime bundle and publish checksums.
