# Versioning

Public binary releases use Git tags beginning with `v`.

When a new compiled build is published, keep the Git tag, release title, changelog entry, packaged ZIP name, and application metadata aligned where possible.
