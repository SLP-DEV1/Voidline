# Packaging

The release workflow packages the tracked runtime payload into a versioned ZIP and generates SHA-256 checksums. Runtime payloads include the executable, application DLL, dependency DLLs, runtime JSON files, and native files under `runtimes/`.
